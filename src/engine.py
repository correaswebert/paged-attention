import typer
import torch
import tokenizer


def tokenize(prompt: str):
    ...


def forward():
    ...


def main(prompt: str):
    tokenizer.encode(prompt)


if __name__ == "__main__":
    typer.run(main)
